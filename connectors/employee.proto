syntax = "proto3";

package connectors;
option go_package = "../connectors;pb";

message Request {
  string body = 1;
}

message Response {
  string body = 1;
}

service EmployeeService {
  rpc AddNewEmployee(Request) returns (Response) {}
}